version: '3.8'
services:
  lead-qualification:
    build: .
    ports:
      - "8000:8000"
    environment:
      - OPENAI_API_KEY=${OPENAI_API_KEY}
      - HUBSPOT_API_KEY=${HUBSPOT_API_KEY}
      - MAKE_WEBHOOK_URL=${MAKE_WEBHOOK_URL}
